#include <iostream>
#include "inputparser.hh"
#include "manager.hh"

int main(int argc, char** argv) {
    try {
        
    } catch (...) {
        std::cerr << "Unknown error" << std::endl;
        return -1;
    }
}